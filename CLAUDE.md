# ds-skills

Read [README.md](README.md) first.

## Pushing

This repo lives on the **work** GitHub account, `tiagopedras-twinkl`. Push as
that account — `gh auth switch --hostname github.com --user tiagopedras-twinkl`
if it isn't already active.
